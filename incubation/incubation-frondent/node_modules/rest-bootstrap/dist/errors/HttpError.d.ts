export declare class HttpError extends Error {
    private _code;
    private _errors;
    constructor(code: number, message: string, errors?: any[]);
    get code(): number;
    get errors(): any[];
    toJSON(): {
        code: number;
        message: string;
        errors: any[];
    };
}
